# Environment variables that need to be available in all zsh sessions
# (including non-interactive scripts and subshells).
# Interactive-only settings (aliases, completions, prompt) belong in .zshrc.
